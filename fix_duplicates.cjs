const fs = require("fs"); const path = require("path"); const replaceInFile = (file, regex, replaceWith) => { let content = fs.readFileSync(file, "utf8"); let newContent = content.replace(regex, replaceWith); if (content !== newContent) { fs.writeFileSync(file, newContent); console.log("Fixed " + file); } }; const dir = "d:/Vyombiz/VyomBiz/src/pages/startup/NGOTrust"; fs.readdirSync(dir).filter(f => f.endsWith(".jsx")).forEach(file => { replaceInFile(path.join(dir, file), /<Link onClick=\{\(\) => window\.scrollTo\(\{ top: 0, behavior: "smooth" \}\)\} to="\/general-info"/g, "<Link to=\"/general-info\""); });
