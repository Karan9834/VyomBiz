import React from 'react';
import PageTemplate from '../../components/common/PageTemplate';

const RefundPolicy = () => {
  return <PageTemplate title='RefundPolicy' />;
};

export default RefundPolicy;
